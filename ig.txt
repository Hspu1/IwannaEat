swagger and RDBMS only

EndPoint1
- user_id; order_id (or amount for testing)
---------------------------
counting the total_amount considering taxes, delivery, cashback etc;
insert user_id and total_amount with status PENDING (wallet_top_ups model)
and status CREATED (orders model)
---------------------------
- total_amount w/ wallet_top_ups.id

MOCK frontend (ts is frontend's business)
- user_id; cards_data; wallet_top_ups.id; total_amount
---------------------------
sync post to stripe using cards_data and total_amount with
idempotency_key=wallet_top_ups.id and metadata with wallet_top_ups.id and user_id
with the flag capture_method=manual
---------------------------
- 400 if no money
- PaymentIntent ID if yah money

EndPoint2
- PaymentIntent ID
- wallet_top_ups.id
---------------------------
insert PaymentIntent ID (wallet_top_ups model w/ unique field)
and payload {"payment_intent_id": X, "wallet_top_ups_id": Y}
with PROCESS_ORDER_PAYMENT event_type (all of ts for outbox_events model)
---------------------------
- 202

Worker (ideally after CDC + EB to ts) powered by stamina and circuit breaker
post to stripe using PaymentIntent ID and idempotency_key=capture_<outbox_events.id>

INTERNAL EndPoint1
- stripe webhook
---------------------------
DANGER: RACE CONDITION
0) check signature; event type; metadata; uuid format
1) select and check top up status
2) update top up status
3) update balance and handle exceptions:
asyncpg.exceptions.DataError "value out of int64 range"
asyncpg.exceptions.NumericValueOutOfRangeError "bigint out of range"
4) insert some shi to outbox (future CDC + EB to analytics)
---------------------------
- 200

Cron Jobs:
- clean PENDINGs (wallet_top_ups model) and CREATED (orders model)
after some existing time (wb indexes here huh)
- clean outbox_events model after some existing time
